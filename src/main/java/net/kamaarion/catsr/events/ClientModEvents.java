package net.kamaarion.catsr.events;

import net.kamaarion.catsr.CATSR;
/*import net.kamaarion.catsr.items.curios.sundrowned_tome.SundrownedTomeRenderer;
import net.kamaarion.catsr.registries.CATSRItemRegistry;
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.event.lifecycle.FMLClientSetupEvent;
import top.theillusivec4.curios.api.client.CuriosRendererRegistry;

// 1. Tells Forge this class handles client-side lifecycle setup events
@Mod.EventBusSubscriber(modid = CATSR.MODID, bus = Mod.EventBusSubscriber.Bus.MOD, value = Dist.CLIENT)
public class ClientModEvents {

    // 2. Listens for the client setup phase during mod loading [3]
    @SubscribeEvent
    public static void onClientSetup(final FMLClientSetupEvent event) {
        // 3. Thread-safe execution to safely register layout render layers on the client thread [3]
        event.enqueueWork(() -> {
            CuriosRendererRegistry.register(
                    CATSRItemRegistry.SUNDROWNED_TOME.get(),
                    SundrownedTomeRenderer::new
            );
        });
    }
}*/
